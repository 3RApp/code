import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import { DefaultBehaviourForm } from './5.52';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <DefaultBehaviourForm />
  </React.StrictMode>
);
