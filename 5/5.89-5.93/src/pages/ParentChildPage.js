import {ParentChild} from '../business';

import './Common.css';

export const ParentChildPage = () => {
    
    return (
        <div>
            <h1>Родитель-Дочерний</h1>
            <section className='container'>
                <ParentChild />
            </section>
        </div>
    );
};