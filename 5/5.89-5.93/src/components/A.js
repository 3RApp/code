// Этот простой конечный компонент является дочерним компонентом для бизнес-компонент ParentChild. 
// Он расположен src\business\ParentChild.js
// A.js
export const A = ({ text }) => {
    return (
        <div>{text}</div>
    );
};